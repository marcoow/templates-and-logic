import { gte } from 'templates-and-logic/helpers/gte';
import { module, test } from 'qunit';

module('Unit | Helper | gte');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gte([42]);
  assert.ok(result);
});

